const { runTests } = require('./../utils.js')
const { testCases } = require('./testCases.json')

// given and array and a number N
// remove the first N elements from the array

// case:
// [1, 2, 3], 2

// returns:
// [3]

function removeFirst(arr, num) {}

runTests(removeFirst, testCases)
